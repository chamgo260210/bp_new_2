# API contract audit

- Status: Current
- Verified HEAD: `fd30d55856dd3f266abadea79232c834358abc91`
- Verified Date: 2026-07-24
- Owners: Backend/API
- Related Source: controllers, DTOs, frontend API modules, OpenAPI `0.10.0-phase10`
- Supersedes: Phase-local contract audit notes
- Known Limitations: no generated controller/OpenAPI semantic diff exists

## Result

- Redocly CLI 2.20.5: valid, 0 errors, 0 warnings.
- All controller path families are represented in OpenAPI.
- All OpenAPI path families have a matching controller.
- Frontend uses the documented `/api/v1` families through the shared client.
- Multipart document upload, 204-style logout handling, job type query, auth errors, owner 404, and start/result DTOs are covered.
- Entities are not exposed directly.

## Risks

- Path parity was audited from annotations and YAML, not generated bytecode; method/schema drift can still occur.
- Project PATCH exists in the controller and OpenAPI but has limited frontend use.
- `versionNumber` (business version) and JPA lock version are distinct; API exposes business version only.
- Idempotency is server-derived for current starts; external `Idempotency-Key` is allowed by CORS but is not a universal API behavior.
- API has no pagination because current lists are education-scale; this is a contract-change candidate if volumes grow.

Classification: current parity is `ACCEPTABLE_FOR_PROJECT`; automated semantic diff is `POST_PHASE11_AUTOMATION`.
