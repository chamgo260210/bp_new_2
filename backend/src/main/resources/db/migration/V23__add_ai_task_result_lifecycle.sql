ALTER TABLE analysis_jobs ADD COLUMN rerun_of_job_id BIGINT;
ALTER TABLE analysis_jobs
    ADD CONSTRAINT fk_job_rerun_of
    FOREIGN KEY (rerun_of_job_id) REFERENCES analysis_jobs(id);
CREATE INDEX idx_job_rerun_of ON analysis_jobs(rerun_of_job_id);

CREATE TABLE ai_task_results (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    analysis_job_id BIGINT NOT NULL UNIQUE,
    task_type VARCHAR(40) NOT NULL,
    schema_version VARCHAR(20) NOT NULL,
    request_id VARCHAR(100) NOT NULL,
    result_json TEXT NOT NULL,
    handler VARCHAR(100) NOT NULL,
    handler_version VARCHAR(40) NOT NULL,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL,
    deleted_at TIMESTAMP,
    version BIGINT NOT NULL DEFAULT 0,
    CONSTRAINT fk_ai_task_result_job
        FOREIGN KEY (analysis_job_id) REFERENCES analysis_jobs(id)
);
